import { createSlice } from "@reduxjs/toolkit";

const leaderboardIdSlice = createSlice({
    name: "leaderboardId",
    initialState: {},
    reducers: {}
})

export default leaderboardIdSlice.reducer